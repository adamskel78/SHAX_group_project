CREATE TABLE review_id_table (
  id INTEGER,
  age INTEGER,
  gender INTEGER,
  height INTEGER,
  weight_patient INTEGER,
  ap_hi INTEGER,
  ap_lo INTEGER,
  cholesterol INTEGER,
  gluc INTEGER,
  smoke INTEGER
  alco INTEGER,
  active INTEGER,
  cardio INTEGER
);
